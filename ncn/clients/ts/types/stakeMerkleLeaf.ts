/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  getAddressDecoder,
  getAddressEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
} from "@solana/kit";

export type StakeMerkleLeaf = {
  /** Wallet designated for governance voting for the stake account. */
  votingWallet: Address;
  /** The stake account address. */
  stakeAccount: Address;
  /** Active delegated stake amount. */
  activeStake: bigint;
};

export type StakeMerkleLeafArgs = {
  /** Wallet designated for governance voting for the stake account. */
  votingWallet: Address;
  /** The stake account address. */
  stakeAccount: Address;
  /** Active delegated stake amount. */
  activeStake: number | bigint;
};

export function getStakeMerkleLeafEncoder(): FixedSizeEncoder<StakeMerkleLeafArgs> {
  return getStructEncoder([
    ["votingWallet", getAddressEncoder()],
    ["stakeAccount", getAddressEncoder()],
    ["activeStake", getU64Encoder()],
  ]);
}

export function getStakeMerkleLeafDecoder(): FixedSizeDecoder<StakeMerkleLeaf> {
  return getStructDecoder([
    ["votingWallet", getAddressDecoder()],
    ["stakeAccount", getAddressDecoder()],
    ["activeStake", getU64Decoder()],
  ]);
}

export function getStakeMerkleLeafCodec(): FixedSizeCodec<
  StakeMerkleLeafArgs,
  StakeMerkleLeaf
> {
  return combineCodec(getStakeMerkleLeafEncoder(), getStakeMerkleLeafDecoder());
}
