/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NCN_SNAPSHOT_PROGRAM_ADDRESS } from "../programs";

export const FINALIZE_BALLOT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [212, 43, 85, 58, 158, 34, 41, 42],
);

export function getFinalizeBallotDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINALIZE_BALLOT_DISCRIMINATOR,
  );
}

export type FinalizeBallotInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountBallotBox extends string | AccountMeta<string> = string,
  TAccountConsensusResult extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountBallotBox extends string
        ? ReadonlyAccount<TAccountBallotBox>
        : TAccountBallotBox,
      TAccountConsensusResult extends string
        ? WritableAccount<TAccountConsensusResult>
        : TAccountConsensusResult,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FinalizeBallotInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinalizeBallotInstructionDataArgs = {};

export function getFinalizeBallotInstructionDataEncoder(): FixedSizeEncoder<FinalizeBallotInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: FINALIZE_BALLOT_DISCRIMINATOR }),
  );
}

export function getFinalizeBallotInstructionDataDecoder(): FixedSizeDecoder<FinalizeBallotInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinalizeBallotInstructionDataCodec(): FixedSizeCodec<
  FinalizeBallotInstructionDataArgs,
  FinalizeBallotInstructionData
> {
  return combineCodec(
    getFinalizeBallotInstructionDataEncoder(),
    getFinalizeBallotInstructionDataDecoder(),
  );
}

export type FinalizeBallotInput<
  TAccountPayer extends string = string,
  TAccountBallotBox extends string = string,
  TAccountConsensusResult extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  ballotBox: Address<TAccountBallotBox>;
  consensusResult: Address<TAccountConsensusResult>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getFinalizeBallotInstruction<
  TAccountPayer extends string,
  TAccountBallotBox extends string,
  TAccountConsensusResult extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
>(
  input: FinalizeBallotInput<
    TAccountPayer,
    TAccountBallotBox,
    TAccountConsensusResult,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FinalizeBallotInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountBallotBox,
  TAccountConsensusResult,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? NCN_SNAPSHOT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    ballotBox: { value: input.ballotBox ?? null, isWritable: false },
    consensusResult: { value: input.consensusResult ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("payer", accounts.payer),
      getAccountMeta("ballotBox", accounts.ballotBox),
      getAccountMeta("consensusResult", accounts.consensusResult),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getFinalizeBallotInstructionDataEncoder().encode({}),
    programAddress,
  } as FinalizeBallotInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountBallotBox,
    TAccountConsensusResult,
    TAccountSystemProgram
  >);
}

export type ParsedFinalizeBallotInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    ballotBox: TAccountMetas[1];
    consensusResult: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: FinalizeBallotInstructionData;
};

export function parseFinalizeBallotInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinalizeBallotInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      ballotBox: getNextAccount(),
      consensusResult: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFinalizeBallotInstructionDataDecoder().decode(instruction.data),
  };
}
