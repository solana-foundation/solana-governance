/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NCN_SNAPSHOT_PROGRAM_ADDRESS } from "../programs";
import {
  getBallotDecoder,
  getBallotEncoder,
  type Ballot,
  type BallotArgs,
} from "../types";

export const SET_TIE_BREAKER_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [228, 10, 240, 130, 193, 58, 103, 181],
);

export function getSetTieBreakerDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_TIE_BREAKER_DISCRIMINATOR,
  );
}

export type SetTieBreakerInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountTieBreakerAdmin extends string | AccountMeta<string> = string,
  TAccountBallotBox extends string | AccountMeta<string> = string,
  TAccountProgramConfig extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountTieBreakerAdmin extends string
        ? ReadonlySignerAccount<TAccountTieBreakerAdmin> &
            AccountSignerMeta<TAccountTieBreakerAdmin>
        : TAccountTieBreakerAdmin,
      TAccountBallotBox extends string
        ? WritableAccount<TAccountBallotBox>
        : TAccountBallotBox,
      TAccountProgramConfig extends string
        ? ReadonlyAccount<TAccountProgramConfig>
        : TAccountProgramConfig,
      ...TRemainingAccounts,
    ]
  >;

export type SetTieBreakerInstructionData = {
  discriminator: ReadonlyUint8Array;
  ballot: Ballot;
};

export type SetTieBreakerInstructionDataArgs = { ballot: BallotArgs };

export function getSetTieBreakerInstructionDataEncoder(): FixedSizeEncoder<SetTieBreakerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["ballot", getBallotEncoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_TIE_BREAKER_DISCRIMINATOR }),
  );
}

export function getSetTieBreakerInstructionDataDecoder(): FixedSizeDecoder<SetTieBreakerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["ballot", getBallotDecoder()],
  ]);
}

export function getSetTieBreakerInstructionDataCodec(): FixedSizeCodec<
  SetTieBreakerInstructionDataArgs,
  SetTieBreakerInstructionData
> {
  return combineCodec(
    getSetTieBreakerInstructionDataEncoder(),
    getSetTieBreakerInstructionDataDecoder(),
  );
}

export type SetTieBreakerInput<
  TAccountTieBreakerAdmin extends string = string,
  TAccountBallotBox extends string = string,
  TAccountProgramConfig extends string = string,
> = {
  tieBreakerAdmin: TransactionSigner<TAccountTieBreakerAdmin>;
  ballotBox: Address<TAccountBallotBox>;
  programConfig: Address<TAccountProgramConfig>;
  ballot: SetTieBreakerInstructionDataArgs["ballot"];
};

export function getSetTieBreakerInstruction<
  TAccountTieBreakerAdmin extends string,
  TAccountBallotBox extends string,
  TAccountProgramConfig extends string,
  TProgramAddress extends Address = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
>(
  input: SetTieBreakerInput<
    TAccountTieBreakerAdmin,
    TAccountBallotBox,
    TAccountProgramConfig
  >,
  config?: { programAddress?: TProgramAddress },
): SetTieBreakerInstruction<
  TProgramAddress,
  TAccountTieBreakerAdmin,
  TAccountBallotBox,
  TAccountProgramConfig
> {
  // Program address.
  const programAddress = config?.programAddress ?? NCN_SNAPSHOT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    tieBreakerAdmin: {
      value: input.tieBreakerAdmin ?? null,
      isWritable: false,
    },
    ballotBox: { value: input.ballotBox ?? null, isWritable: true },
    programConfig: { value: input.programConfig ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("tieBreakerAdmin", accounts.tieBreakerAdmin),
      getAccountMeta("ballotBox", accounts.ballotBox),
      getAccountMeta("programConfig", accounts.programConfig),
    ],
    data: getSetTieBreakerInstructionDataEncoder().encode(
      args as SetTieBreakerInstructionDataArgs,
    ),
    programAddress,
  } as SetTieBreakerInstruction<
    TProgramAddress,
    TAccountTieBreakerAdmin,
    TAccountBallotBox,
    TAccountProgramConfig
  >);
}

export type ParsedSetTieBreakerInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    tieBreakerAdmin: TAccountMetas[0];
    ballotBox: TAccountMetas[1];
    programConfig: TAccountMetas[2];
  };
  data: SetTieBreakerInstructionData;
};

export function parseSetTieBreakerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetTieBreakerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      tieBreakerAdmin: getNextAccount(),
      ballotBox: getNextAccount(),
      programConfig: getNextAccount(),
    },
    data: getSetTieBreakerInstructionDataDecoder().decode(instruction.data),
  };
}
