/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { NCN_SNAPSHOT_PROGRAM_ADDRESS } from "../programs";

export const REMOVE_VOTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  32, 187, 23, 3, 156, 232, 55, 177,
]);

export function getRemoveVoteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(REMOVE_VOTE_DISCRIMINATOR);
}

export type RemoveVoteInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountOperator extends string | AccountMeta<string> = string,
  TAccountBallotBox extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOperator extends string
        ? ReadonlySignerAccount<TAccountOperator> &
            AccountSignerMeta<TAccountOperator>
        : TAccountOperator,
      TAccountBallotBox extends string
        ? WritableAccount<TAccountBallotBox>
        : TAccountBallotBox,
      ...TRemainingAccounts,
    ]
  >;

export type RemoveVoteInstructionData = { discriminator: ReadonlyUint8Array };

export type RemoveVoteInstructionDataArgs = {};

export function getRemoveVoteInstructionDataEncoder(): FixedSizeEncoder<RemoveVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: REMOVE_VOTE_DISCRIMINATOR }),
  );
}

export function getRemoveVoteInstructionDataDecoder(): FixedSizeDecoder<RemoveVoteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRemoveVoteInstructionDataCodec(): FixedSizeCodec<
  RemoveVoteInstructionDataArgs,
  RemoveVoteInstructionData
> {
  return combineCodec(
    getRemoveVoteInstructionDataEncoder(),
    getRemoveVoteInstructionDataDecoder(),
  );
}

export type RemoveVoteInput<
  TAccountOperator extends string = string,
  TAccountBallotBox extends string = string,
> = {
  operator: TransactionSigner<TAccountOperator>;
  ballotBox: Address<TAccountBallotBox>;
};

export function getRemoveVoteInstruction<
  TAccountOperator extends string,
  TAccountBallotBox extends string,
  TProgramAddress extends Address = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
>(
  input: RemoveVoteInput<TAccountOperator, TAccountBallotBox>,
  config?: { programAddress?: TProgramAddress },
): RemoveVoteInstruction<TProgramAddress, TAccountOperator, TAccountBallotBox> {
  // Program address.
  const programAddress = config?.programAddress ?? NCN_SNAPSHOT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    operator: { value: input.operator ?? null, isWritable: false },
    ballotBox: { value: input.ballotBox ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("operator", accounts.operator),
      getAccountMeta("ballotBox", accounts.ballotBox),
    ],
    data: getRemoveVoteInstructionDataEncoder().encode({}),
    programAddress,
  } as RemoveVoteInstruction<
    TProgramAddress,
    TAccountOperator,
    TAccountBallotBox
  >);
}

export type ParsedRemoveVoteInstruction<
  TProgram extends string = typeof NCN_SNAPSHOT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    operator: TAccountMetas[0];
    ballotBox: TAccountMetas[1];
  };
  data: RemoveVoteInstructionData;
};

export function parseRemoveVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRemoveVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { operator: getNextAccount(), ballotBox: getNextAccount() },
    data: getRemoveVoteInstructionDataDecoder().decode(instruction.data),
  };
}
