/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type ReadonlyUint8Array,
} from "@solana/kit";

export type MetaMerkleLeaf = {
  /** Wallet designated for governance voting for the vote account. */
  votingWallet: Address;
  /** Validator's vote account. */
  voteAccount: Address;
  /**
   * Root hash of the StakeMerkleTree, representing all active stake accounts
   * delegated to the current vote account.
   */
  stakeMerkleRoot: ReadonlyUint8Array;
  /** Total active delegated stake under this vote account. */
  activeStake: bigint;
};

export type MetaMerkleLeafArgs = {
  /** Wallet designated for governance voting for the vote account. */
  votingWallet: Address;
  /** Validator's vote account. */
  voteAccount: Address;
  /**
   * Root hash of the StakeMerkleTree, representing all active stake accounts
   * delegated to the current vote account.
   */
  stakeMerkleRoot: ReadonlyUint8Array;
  /** Total active delegated stake under this vote account. */
  activeStake: number | bigint;
};

export function getMetaMerkleLeafEncoder(): FixedSizeEncoder<MetaMerkleLeafArgs> {
  return getStructEncoder([
    ["votingWallet", getAddressEncoder()],
    ["voteAccount", getAddressEncoder()],
    ["stakeMerkleRoot", fixEncoderSize(getBytesEncoder(), 32)],
    ["activeStake", getU64Encoder()],
  ]);
}

export function getMetaMerkleLeafDecoder(): FixedSizeDecoder<MetaMerkleLeaf> {
  return getStructDecoder([
    ["votingWallet", getAddressDecoder()],
    ["voteAccount", getAddressDecoder()],
    ["stakeMerkleRoot", fixDecoderSize(getBytesDecoder(), 32)],
    ["activeStake", getU64Decoder()],
  ]);
}

export function getMetaMerkleLeafCodec(): FixedSizeCodec<
  MetaMerkleLeafArgs,
  MetaMerkleLeaf
> {
  return combineCodec(getMetaMerkleLeafEncoder(), getMetaMerkleLeafDecoder());
}
