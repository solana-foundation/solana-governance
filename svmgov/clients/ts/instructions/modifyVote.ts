/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findVotePda } from "../pdas";
import { SVMGOV_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const MODIFY_VOTE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  116, 52, 102, 0, 121, 145, 27, 139,
]);

export function getModifyVoteDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(MODIFY_VOTE_DISCRIMINATOR);
}

export type ModifyVoteInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountVote extends string | AccountMeta<string> = string,
  TAccountSplVoteAccount extends string | AccountMeta<string> = string,
  TAccountSnapshotProgram extends string | AccountMeta<string> = string,
  TAccountConsensusResult extends string | AccountMeta<string> = string,
  TAccountMetaMerkleProof extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? ReadonlySignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      TAccountVote extends string
        ? WritableAccount<TAccountVote>
        : TAccountVote,
      TAccountSplVoteAccount extends string
        ? ReadonlyAccount<TAccountSplVoteAccount>
        : TAccountSplVoteAccount,
      TAccountSnapshotProgram extends string
        ? ReadonlyAccount<TAccountSnapshotProgram>
        : TAccountSnapshotProgram,
      TAccountConsensusResult extends string
        ? ReadonlyAccount<TAccountConsensusResult>
        : TAccountConsensusResult,
      TAccountMetaMerkleProof extends string
        ? ReadonlyAccount<TAccountMetaMerkleProof>
        : TAccountMetaMerkleProof,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ModifyVoteInstructionData = {
  discriminator: ReadonlyUint8Array;
  forVotesBp: bigint;
  againstVotesBp: bigint;
  abstainVotesBp: bigint;
};

export type ModifyVoteInstructionDataArgs = {
  forVotesBp: number | bigint;
  againstVotesBp: number | bigint;
  abstainVotesBp: number | bigint;
};

export function getModifyVoteInstructionDataEncoder(): FixedSizeEncoder<ModifyVoteInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["forVotesBp", getU64Encoder()],
      ["againstVotesBp", getU64Encoder()],
      ["abstainVotesBp", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: MODIFY_VOTE_DISCRIMINATOR }),
  );
}

export function getModifyVoteInstructionDataDecoder(): FixedSizeDecoder<ModifyVoteInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["forVotesBp", getU64Decoder()],
    ["againstVotesBp", getU64Decoder()],
    ["abstainVotesBp", getU64Decoder()],
  ]);
}

export function getModifyVoteInstructionDataCodec(): FixedSizeCodec<
  ModifyVoteInstructionDataArgs,
  ModifyVoteInstructionData
> {
  return combineCodec(
    getModifyVoteInstructionDataEncoder(),
    getModifyVoteInstructionDataDecoder(),
  );
}

export type ModifyVoteAsyncInput<
  TAccountSigner extends string = string,
  TAccountProposal extends string = string,
  TAccountVote extends string = string,
  TAccountSplVoteAccount extends string = string,
  TAccountSnapshotProgram extends string = string,
  TAccountConsensusResult extends string = string,
  TAccountMetaMerkleProof extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  proposal: Address<TAccountProposal>;
  vote?: Address<TAccountVote>;
  /**
   * enforced here. The signer-to-vote-account binding is proved in the handler
   * via the meta merkle proof: voting_wallet == signer AND vote_account ==
   * spl_vote_account, both anchored to proposal.consensus_result.
   */
  splVoteAccount: Address<TAccountSplVoteAccount>;
  snapshotProgram: Address<TAccountSnapshotProgram>;
  consensusResult: Address<TAccountConsensusResult>;
  metaMerkleProof: Address<TAccountMetaMerkleProof>;
  systemProgram?: Address<TAccountSystemProgram>;
  forVotesBp: ModifyVoteInstructionDataArgs["forVotesBp"];
  againstVotesBp: ModifyVoteInstructionDataArgs["againstVotesBp"];
  abstainVotesBp: ModifyVoteInstructionDataArgs["abstainVotesBp"];
};

export async function getModifyVoteInstructionAsync<
  TAccountSigner extends string,
  TAccountProposal extends string,
  TAccountVote extends string,
  TAccountSplVoteAccount extends string,
  TAccountSnapshotProgram extends string,
  TAccountConsensusResult extends string,
  TAccountMetaMerkleProof extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ModifyVoteAsyncInput<
    TAccountSigner,
    TAccountProposal,
    TAccountVote,
    TAccountSplVoteAccount,
    TAccountSnapshotProgram,
    TAccountConsensusResult,
    TAccountMetaMerkleProof,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  ModifyVoteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountVote,
    TAccountSplVoteAccount,
    TAccountSnapshotProgram,
    TAccountConsensusResult,
    TAccountMetaMerkleProof,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    proposal: { value: input.proposal ?? null, isWritable: true },
    vote: { value: input.vote ?? null, isWritable: true },
    splVoteAccount: { value: input.splVoteAccount ?? null, isWritable: false },
    snapshotProgram: {
      value: input.snapshotProgram ?? null,
      isWritable: false,
    },
    consensusResult: {
      value: input.consensusResult ?? null,
      isWritable: false,
    },
    metaMerkleProof: {
      value: input.metaMerkleProof ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.vote.value) {
    accounts.vote.value = await findVotePda({
      proposal: getAddressFromResolvedInstructionAccount(
        "proposal",
        accounts.proposal.value,
      ),
      splVoteAccount: getAddressFromResolvedInstructionAccount(
        "splVoteAccount",
        accounts.splVoteAccount.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("vote", accounts.vote),
      getAccountMeta("splVoteAccount", accounts.splVoteAccount),
      getAccountMeta("snapshotProgram", accounts.snapshotProgram),
      getAccountMeta("consensusResult", accounts.consensusResult),
      getAccountMeta("metaMerkleProof", accounts.metaMerkleProof),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getModifyVoteInstructionDataEncoder().encode(
      args as ModifyVoteInstructionDataArgs,
    ),
    programAddress,
  } as ModifyVoteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountVote,
    TAccountSplVoteAccount,
    TAccountSnapshotProgram,
    TAccountConsensusResult,
    TAccountMetaMerkleProof,
    TAccountSystemProgram
  >);
}

export type ModifyVoteInput<
  TAccountSigner extends string = string,
  TAccountProposal extends string = string,
  TAccountVote extends string = string,
  TAccountSplVoteAccount extends string = string,
  TAccountSnapshotProgram extends string = string,
  TAccountConsensusResult extends string = string,
  TAccountMetaMerkleProof extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  proposal: Address<TAccountProposal>;
  vote: Address<TAccountVote>;
  /**
   * enforced here. The signer-to-vote-account binding is proved in the handler
   * via the meta merkle proof: voting_wallet == signer AND vote_account ==
   * spl_vote_account, both anchored to proposal.consensus_result.
   */
  splVoteAccount: Address<TAccountSplVoteAccount>;
  snapshotProgram: Address<TAccountSnapshotProgram>;
  consensusResult: Address<TAccountConsensusResult>;
  metaMerkleProof: Address<TAccountMetaMerkleProof>;
  systemProgram?: Address<TAccountSystemProgram>;
  forVotesBp: ModifyVoteInstructionDataArgs["forVotesBp"];
  againstVotesBp: ModifyVoteInstructionDataArgs["againstVotesBp"];
  abstainVotesBp: ModifyVoteInstructionDataArgs["abstainVotesBp"];
};

export function getModifyVoteInstruction<
  TAccountSigner extends string,
  TAccountProposal extends string,
  TAccountVote extends string,
  TAccountSplVoteAccount extends string,
  TAccountSnapshotProgram extends string,
  TAccountConsensusResult extends string,
  TAccountMetaMerkleProof extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: ModifyVoteInput<
    TAccountSigner,
    TAccountProposal,
    TAccountVote,
    TAccountSplVoteAccount,
    TAccountSnapshotProgram,
    TAccountConsensusResult,
    TAccountMetaMerkleProof,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ModifyVoteInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountProposal,
  TAccountVote,
  TAccountSplVoteAccount,
  TAccountSnapshotProgram,
  TAccountConsensusResult,
  TAccountMetaMerkleProof,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: false },
    proposal: { value: input.proposal ?? null, isWritable: true },
    vote: { value: input.vote ?? null, isWritable: true },
    splVoteAccount: { value: input.splVoteAccount ?? null, isWritable: false },
    snapshotProgram: {
      value: input.snapshotProgram ?? null,
      isWritable: false,
    },
    consensusResult: {
      value: input.consensusResult ?? null,
      isWritable: false,
    },
    metaMerkleProof: {
      value: input.metaMerkleProof ?? null,
      isWritable: false,
    },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("vote", accounts.vote),
      getAccountMeta("splVoteAccount", accounts.splVoteAccount),
      getAccountMeta("snapshotProgram", accounts.snapshotProgram),
      getAccountMeta("consensusResult", accounts.consensusResult),
      getAccountMeta("metaMerkleProof", accounts.metaMerkleProof),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getModifyVoteInstructionDataEncoder().encode(
      args as ModifyVoteInstructionDataArgs,
    ),
    programAddress,
  } as ModifyVoteInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountVote,
    TAccountSplVoteAccount,
    TAccountSnapshotProgram,
    TAccountConsensusResult,
    TAccountMetaMerkleProof,
    TAccountSystemProgram
  >);
}

export type ParsedModifyVoteInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    proposal: TAccountMetas[1];
    vote: TAccountMetas[2];
    /**
     * enforced here. The signer-to-vote-account binding is proved in the handler
     * via the meta merkle proof: voting_wallet == signer AND vote_account ==
     * spl_vote_account, both anchored to proposal.consensus_result.
     */
    splVoteAccount: TAccountMetas[3];
    snapshotProgram: TAccountMetas[4];
    consensusResult: TAccountMetas[5];
    metaMerkleProof: TAccountMetas[6];
    systemProgram: TAccountMetas[7];
  };
  data: ModifyVoteInstructionData;
};

export function parseModifyVoteInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedModifyVoteInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 8) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 8,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      proposal: getNextAccount(),
      vote: getNextAccount(),
      splVoteAccount: getNextAccount(),
      snapshotProgram: getNextAccount(),
      consensusResult: getNextAccount(),
      metaMerkleProof: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getModifyVoteInstructionDataDecoder().decode(instruction.data),
  };
}
