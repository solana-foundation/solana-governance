/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findGlobalConfigPda, findProgramConfigPda } from "../pdas";
import { SVMGOV_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const RETALLY_SUPPORT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array(
  [132, 55, 145, 255, 12, 163, 151, 141],
);

export function getRetallySupportDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    RETALLY_SUPPORT_DISCRIMINATOR,
  );
}

export type RetallySupportInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountBallotBox extends string | AccountMeta<string> = string,
  TAccountBallotProgram extends string | AccountMeta<string> = string,
  TAccountProgramConfig extends string | AccountMeta<string> = string,
  TAccountGlobalConfig extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountProposal extends string
        ? WritableAccount<TAccountProposal>
        : TAccountProposal,
      TAccountBallotBox extends string
        ? WritableAccount<TAccountBallotBox>
        : TAccountBallotBox,
      TAccountBallotProgram extends string
        ? ReadonlyAccount<TAccountBallotProgram>
        : TAccountBallotProgram,
      TAccountProgramConfig extends string
        ? ReadonlyAccount<TAccountProgramConfig>
        : TAccountProgramConfig,
      TAccountGlobalConfig extends string
        ? ReadonlyAccount<TAccountGlobalConfig>
        : TAccountGlobalConfig,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type RetallySupportInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type RetallySupportInstructionDataArgs = {};

export function getRetallySupportInstructionDataEncoder(): FixedSizeEncoder<RetallySupportInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: RETALLY_SUPPORT_DISCRIMINATOR }),
  );
}

export function getRetallySupportInstructionDataDecoder(): FixedSizeDecoder<RetallySupportInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getRetallySupportInstructionDataCodec(): FixedSizeCodec<
  RetallySupportInstructionDataArgs,
  RetallySupportInstructionData
> {
  return combineCodec(
    getRetallySupportInstructionDataEncoder(),
    getRetallySupportInstructionDataDecoder(),
  );
}

export type RetallySupportAsyncInput<
  TAccountSigner extends string = string,
  TAccountProposal extends string = string,
  TAccountBallotBox extends string = string,
  TAccountBallotProgram extends string = string,
  TAccountProgramConfig extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Any caller. Pays only the tx fee — plus the ballot-box rent iff this
   * retally activates voting and the ballot box does not exist yet.
   */
  signer: TransactionSigner<TAccountSigner>;
  proposal: Address<TAccountProposal>;
  ballotBox: Address<TAccountBallotBox>;
  ballotProgram: Address<TAccountBallotProgram>;
  programConfig?: Address<TAccountProgramConfig>;
  globalConfig?: Address<TAccountGlobalConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getRetallySupportInstructionAsync<
  TAccountSigner extends string,
  TAccountProposal extends string,
  TAccountBallotBox extends string,
  TAccountBallotProgram extends string,
  TAccountProgramConfig extends string,
  TAccountGlobalConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RetallySupportAsyncInput<
    TAccountSigner,
    TAccountProposal,
    TAccountBallotBox,
    TAccountBallotProgram,
    TAccountProgramConfig,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  RetallySupportInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountBallotBox,
    TAccountBallotProgram,
    TAccountProgramConfig,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
    ballotBox: { value: input.ballotBox ?? null, isWritable: true },
    ballotProgram: { value: input.ballotProgram ?? null, isWritable: false },
    programConfig: { value: input.programConfig ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.programConfig.value) {
    accounts.programConfig.value = await findProgramConfigPda({
      programAddress: getAddressFromResolvedInstructionAccount(
        "ballotProgram",
        accounts.ballotProgram.value,
      ),
    });
  }
  if (!accounts.globalConfig.value) {
    accounts.globalConfig.value = await findGlobalConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("ballotBox", accounts.ballotBox),
      getAccountMeta("ballotProgram", accounts.ballotProgram),
      getAccountMeta("programConfig", accounts.programConfig),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRetallySupportInstructionDataEncoder().encode({}),
    programAddress,
  } as RetallySupportInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountBallotBox,
    TAccountBallotProgram,
    TAccountProgramConfig,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >);
}

export type RetallySupportInput<
  TAccountSigner extends string = string,
  TAccountProposal extends string = string,
  TAccountBallotBox extends string = string,
  TAccountBallotProgram extends string = string,
  TAccountProgramConfig extends string = string,
  TAccountGlobalConfig extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * Any caller. Pays only the tx fee — plus the ballot-box rent iff this
   * retally activates voting and the ballot box does not exist yet.
   */
  signer: TransactionSigner<TAccountSigner>;
  proposal: Address<TAccountProposal>;
  ballotBox: Address<TAccountBallotBox>;
  ballotProgram: Address<TAccountBallotProgram>;
  programConfig: Address<TAccountProgramConfig>;
  globalConfig: Address<TAccountGlobalConfig>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getRetallySupportInstruction<
  TAccountSigner extends string,
  TAccountProposal extends string,
  TAccountBallotBox extends string,
  TAccountBallotProgram extends string,
  TAccountProgramConfig extends string,
  TAccountGlobalConfig extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: RetallySupportInput<
    TAccountSigner,
    TAccountProposal,
    TAccountBallotBox,
    TAccountBallotProgram,
    TAccountProgramConfig,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): RetallySupportInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountProposal,
  TAccountBallotBox,
  TAccountBallotProgram,
  TAccountProgramConfig,
  TAccountGlobalConfig,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    proposal: { value: input.proposal ?? null, isWritable: true },
    ballotBox: { value: input.ballotBox ?? null, isWritable: true },
    ballotProgram: { value: input.ballotProgram ?? null, isWritable: false },
    programConfig: { value: input.programConfig ?? null, isWritable: false },
    globalConfig: { value: input.globalConfig ?? null, isWritable: false },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("ballotBox", accounts.ballotBox),
      getAccountMeta("ballotProgram", accounts.ballotProgram),
      getAccountMeta("programConfig", accounts.programConfig),
      getAccountMeta("globalConfig", accounts.globalConfig),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getRetallySupportInstructionDataEncoder().encode({}),
    programAddress,
  } as RetallySupportInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposal,
    TAccountBallotBox,
    TAccountBallotProgram,
    TAccountProgramConfig,
    TAccountGlobalConfig,
    TAccountSystemProgram
  >);
}

export type ParsedRetallySupportInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * Any caller. Pays only the tx fee — plus the ballot-box rent iff this
     * retally activates voting and the ballot box does not exist yet.
     */
    signer: TAccountMetas[0];
    proposal: TAccountMetas[1];
    ballotBox: TAccountMetas[2];
    ballotProgram: TAccountMetas[3];
    programConfig: TAccountMetas[4];
    globalConfig: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: RetallySupportInstructionData;
};

export function parseRetallySupportInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedRetallySupportInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      proposal: getNextAccount(),
      ballotBox: getNextAccount(),
      ballotProgram: getNextAccount(),
      programConfig: getNextAccount(),
      globalConfig: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getRetallySupportInstructionDataDecoder().decode(instruction.data),
  };
}
