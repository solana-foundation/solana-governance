/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findProposalIndexPda } from "../pdas";
import { SVMGOV_PROGRAM_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_INDEX_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([204, 67, 3, 74, 139, 139, 233, 10]);

export function getInitializeIndexDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_INDEX_DISCRIMINATOR,
  );
}

export type InitializeIndexInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountSigner extends string | AccountMeta<string> = string,
  TAccountProposalIndex extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSigner extends string
        ? WritableSignerAccount<TAccountSigner> &
            AccountSignerMeta<TAccountSigner>
        : TAccountSigner,
      TAccountProposalIndex extends string
        ? WritableAccount<TAccountProposalIndex>
        : TAccountProposalIndex,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeIndexInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type InitializeIndexInstructionDataArgs = {};

export function getInitializeIndexInstructionDataEncoder(): FixedSizeEncoder<InitializeIndexInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: INITIALIZE_INDEX_DISCRIMINATOR }),
  );
}

export function getInitializeIndexInstructionDataDecoder(): FixedSizeDecoder<InitializeIndexInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getInitializeIndexInstructionDataCodec(): FixedSizeCodec<
  InitializeIndexInstructionDataArgs,
  InitializeIndexInstructionData
> {
  return combineCodec(
    getInitializeIndexInstructionDataEncoder(),
    getInitializeIndexInstructionDataDecoder(),
  );
}

export type InitializeIndexAsyncInput<
  TAccountSigner extends string = string,
  TAccountProposalIndex extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  proposalIndex?: Address<TAccountProposalIndex>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getInitializeIndexInstructionAsync<
  TAccountSigner extends string,
  TAccountProposalIndex extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeIndexAsyncInput<
    TAccountSigner,
    TAccountProposalIndex,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeIndexInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposalIndex,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    proposalIndex: { value: input.proposalIndex ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.proposalIndex.value) {
    accounts.proposalIndex.value = await findProposalIndexPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposalIndex", accounts.proposalIndex),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeIndexInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeIndexInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposalIndex,
    TAccountSystemProgram
  >);
}

export type InitializeIndexInput<
  TAccountSigner extends string = string,
  TAccountProposalIndex extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  signer: TransactionSigner<TAccountSigner>;
  proposalIndex: Address<TAccountProposalIndex>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getInitializeIndexInstruction<
  TAccountSigner extends string,
  TAccountProposalIndex extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
>(
  input: InitializeIndexInput<
    TAccountSigner,
    TAccountProposalIndex,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeIndexInstruction<
  TProgramAddress,
  TAccountSigner,
  TAccountProposalIndex,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? SVMGOV_PROGRAM_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    signer: { value: input.signer ?? null, isWritable: true },
    proposalIndex: { value: input.proposalIndex ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("signer", accounts.signer),
      getAccountMeta("proposalIndex", accounts.proposalIndex),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeIndexInstructionDataEncoder().encode({}),
    programAddress,
  } as InitializeIndexInstruction<
    TProgramAddress,
    TAccountSigner,
    TAccountProposalIndex,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeIndexInstruction<
  TProgram extends string = typeof SVMGOV_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    signer: TAccountMetas[0];
    proposalIndex: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeIndexInstructionData;
};

export function parseInitializeIndexInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeIndexInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      signer: getNextAccount(),
      proposalIndex: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeIndexInstructionDataDecoder().decode(instruction.data),
  };
}
