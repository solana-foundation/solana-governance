/**
 * Default NCN router/verifier API endpoint, used when no custom URL is configured.
 * Points at the governance NCN router which load-balances across whitelisted verifiers.
 */
export const DEFAULT_NCN_API_URL = "https://ncn-governance.solana.com";
